done - 1000 messages to Kafka in json
done - 1000 messages to Kafka in avro
done - 1000 messages to Kafka in protobuf
done - Size comparison of message in json, avro, and protobuf


done - Baseline latency test (single)
done - CI/CD test - no downtime + ttm

done - Scalability test (linear)
- test auto scaling instances

Crash test (fault tolerance)

Show kowl images of partitions being divided among consumers etc.

kubectl delete -f Pipeline/Services/deploy-projectile-service.yml
kubectl delete -f Pipeline/Services/deploy-ai-service.yml
kubectl delete -f Pipeline/Infrastructure/deploy-mongodb.yml

kubectl scale deployment input-service --replicas=1
kubectl scale deployment collision-service --replicas=1
kubectl scale deployment world-service --replicas=1
kubectl delete pods --all
