apiVersion: v1
kind: Service
metadata:
  name: input-service
spec:
  ports:
    - name: http
      port: 8082
      protocol: TCP
      targetPort: 8080
  selector:
    app: input-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: input-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: input-service
  template:
    metadata:
      labels:
        app: input-service
    spec:
      containers:
        - name: input-service
          image: docker.io/library/10-master-input-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
